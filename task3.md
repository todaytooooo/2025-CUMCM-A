
### ① 确定三枚烟幕弹的投放参数

因为是同一架 FY1，所以三枚弹共享：

$$
\boxed{\theta,\ v}
$$

但分别有自己的：

$$
\boxed{t_{d1},t_{d2},t_{d3}}
$$

以及投放后到起爆的时间：

$$
\boxed{\tau_1,\tau_2,\tau_3}
$$

并满足：

$$
t_{d2}-t_{d1}\ge1,\qquad
t_{d3}-t_{d2}\ge1
$$

所以一组策略可以表示为：

$$
\boxed{
(\theta,v,t_{d1},t_{d2},t_{d3},\tau_1,\tau_2,\tau_3)
}
$$

---

### ② 根据运动学得到三个烟幕的时空位置

对于每枚弹，根据前两问的方法得到：

* 起爆时间 \(t_{ei}=t_{di}+\tau_i\)
* 起爆位置 \(C_i\)
* 起爆后烟幕中心随时间的位置 \(C_i(t)\)

所以得到三个**随时间运动的烟幕球**。

---

### ③ 对每一个时刻，进行“三个烟幕联合遮蔽”的空间判定

这是第三问相比第二问最核心的新内容。

在时刻 \(t\)，考虑：

$$
M(t)\rightarrow \text{真实圆柱目标}
$$

的所有视线。

判断这些视线是否被：

$$
\boxed{\text{烟幕1}\cup\text{烟幕2}\cup\text{烟幕3}}
$$

完全截断。

注意这里是**联合遮蔽**：

* 一枚弹可能只能遮住目标一部分；
* 两枚弹可能从不同空间位置分别遮住不同部分；
* 三枚弹也可能共同实现完全遮蔽。

因此得到：

$$
E(t)=
\begin{cases}
1,&\text{目标完全被联合遮蔽}\\
0,&\text{否则}
\end{cases}
$$

---

### ④ 以总有效遮蔽时间为目标进行优化

最终：

$$
\boxed{
T=\int E(t)\,dt
}
$$

我们要寻找一组参数：

$$
\boxed{
(\theta,v,t_{d1},t_{d2},t_{d3},\tau_1,\tau_2,\tau_3)
}
$$

使 \(T\) 最大。

---



# 执行过程
请根据下面的数学建模思路，编写“烟幕干扰弹投放策略”第三问的 Python 求解程序。

## 一、问题目标

第三问要求：

> 利用无人机 FY1 投放 3 枚烟幕干扰弹，实施对导弹 M1 的干扰，求使 M1 受到有效遮蔽的总时间最长的投放策略。

这里需要特别注意：

**三枚烟幕弹不能简单地分别计算各自的“完全遮蔽时间”然后相加。**

原因是：

1. 三枚烟幕弹由同一架 FY1 投放，因此三枚弹的空间位置受到同一条无人机飞行轨迹的约束；
2. 多枚烟幕弹可能在同一时刻从不同空间位置共同作用，使原本单枚烟幕无法完全遮蔽的目标，在多枚烟幕共同作用下实现完全遮蔽；
3. 因此必须在每一个时刻，对三个烟幕的空间遮蔽效果进行联合判断；
4. 最终目标是最大化三个烟幕共同作用下的“完全遮蔽时间”。

---

## 二、已知运动模型

### 1. 导弹 M1

M1 初始位置：

M1(0) = (20000, 0, 2000)

M1 以 300 m/s 的速度沿指向假目标原点的方向飞行。

因此导弹位置函数为：

M(t) = M1(0) + v_M * t

其中：

v_M = 300 * (-20000, 0, -2000) / sqrt(20000² + 2000²)

即：

M(t) = (20000 - 3000/sqrt(101) * t,
0,
2000 - 300/sqrt(101) * t)

---

### 2. 真实目标

真实目标为半径 7 m、高 10 m 的圆柱体。

底面圆心：

(0, 200, 0)

因此目标区域可以表示为：

T = {(x,y,z):
x² + (y-200)² <= 7²,
0 <= z <= 10}

需要判断的是：

**从导弹 M(t) 出发，真实目标整个圆柱体是否都被三个烟幕共同遮蔽。**

---

### 3. FY1 无人机

FY1 初始位置：

P0 = (17800, 0, 1800)

无人机飞行高度保持 1800 m。

设水平飞行方向角为 θ，速度为 v，则：

70 <= v <= 140

无人机位置：

U(t) =
(17800 + v*t*cos(θ),
v*t*sin(θ),
1800)

其中 θ 和 v 在任务开始后保持不变。

---

## 三、三个烟幕弹的决策变量

优化变量定义为：

x = (
θ,
v,
td1, td2, td3,
τ1, τ2, τ3
)

其中：

* θ：FY1 水平飞行方向；
* v：FY1 飞行速度；
* tdi：第 i 枚烟幕弹投放时间；
* τi：第 i 枚烟幕弹从投放到起爆的时间间隔。

因此第 i 枚烟幕弹的起爆时间：

tei = tdi + τi

三个投弹时间必须满足：

td1 < td2 < td3

并且：

td2 - td1 >= 1

td3 - td2 >= 1

---

## 四、烟幕弹运动模型

第 i 枚烟幕弹在 tdi 时刻从 FY1 投放。

投放点：

Pdi = U(tdi)

烟幕弹水平方向继承无人机速度，竖直方向做自由落体运动。

因此第 i 枚烟幕弹在起爆时的位置为：

Ci = (
17800 + v*tdi*cos(θ),
v*tdi*sin(θ),
1800 - 0.5*g*τi²
)

其中：

g = 9.8 m/s²

起爆后，烟幕中心以 3 m/s 的速度竖直向下运动：

Ci(t) = (
Ci_x,
Ci_y,
Ci_z - 3*(t-tei)
)

烟幕有效时间为起爆后的 20 s。

因此第 i 枚烟幕只在：

tei <= t <= tei + 20

内有效。

烟幕有效区域为以 Ci(t) 为球心、半径 10 m 的球体。

---

# 五、核心：三个烟幕的联合遮蔽判定

这是第三问最重要的部分。

对于某一个时刻 t：

1. 计算导弹位置 M(t)；

2. 计算三个烟幕中心 C1(t)、C2(t)、C3(t)；

3. 对真实目标圆柱体上的空间点 P，考虑视线线段：

   M(t) → P

4. 如果这条线段与至少一个当前有效的烟幕球相交，则认为目标点 P 被遮蔽；

5. 如果真实目标上的所有点 P 都被至少一个烟幕球遮蔽，则认为在时刻 t 真实目标被完全遮蔽。

特别注意：

**这里是三个烟幕遮蔽区域的“联合效果”，不是分别计算三个烟幕的完全遮蔽时间再做简单相加。**

例如：

* 烟幕1只能遮挡目标左侧；
* 烟幕2只能遮挡目标右侧；
* 单独使用任何一个烟幕都无法完全遮蔽；
* 但是二者同时存在时，可以共同遮蔽整个目标。

这种情况必须被程序正确识别。

---

# 六、时间维度上的有效遮蔽时间

定义：

E(t) =
1，若时刻 t 目标被三个烟幕完全遮蔽；
0，否则。

最终有效遮蔽时间：

T = ∫ E(t) dt

程序中可以采用足够小的时间步长进行数值计算，例如：

dt = 0.01 ~ 0.05 s

通过逐时刻判断 E(t)，然后数值积分得到总有效遮蔽时间。

为了提高效率，可以：

* 首先确定导弹可能受到烟幕干扰的时间范围；
* 只在烟幕有效时间范围内进行计算；
* 最后再对结果进行时间步长敏感性分析。

---

# 七、目标函数

将上述过程封装成：

shield_time(x)

输入：

x = (
θ, v,
td1, td2, td3,
τ1, τ2, τ3
)

输出：

三个烟幕共同作用下的总有效遮蔽时间 T。

优化问题为：

max shield_time(x)

满足：

70 <= v <= 140

td2 - td1 >= 1

td3 - td2 >= 1

以及其他由物理运动产生的合理约束。

---

# 八、求解策略

不要一开始直接假定某一种优化算法一定最好。

建议按照以下流程实现：

### Step 1：先实现可靠的单次仿真

给定一组 x：

x = (θ,v,td1,td2,td3,τ1,τ2,τ3)

能够计算：

* 三个投弹点；
* 三个起爆点；
* 三个烟幕随时间的位置；
* 导弹随时间的位置；
* 每个时刻三个烟幕的联合遮蔽情况；
* 最终总有效遮蔽时间。

先确保这个函数正确。

---

### Step 2：进行粗略搜索

在参数空间进行粗网格搜索或者随机采样。

目的不是直接得到最终答案，而是：

* 找到可能的高价值区域；
* 观察目标函数的大致形状；
* 判断三个烟幕在时间上是否倾向于错开；
* 判断是否存在需要多个烟幕同时存在才能实现完全遮蔽的情况；
* 为后续优化算法提供初始参考。

注意：8维空间直接进行非常密集的全网格搜索会产生巨大的计算量，因此可以采用粗粒度网格、分阶段搜索或者随机采样。

---

### Step 3：使用连续变量的启发式优化算法

在粗搜索基础上，再使用一种或多种全局优化算法，例如：

* PSO（粒子群优化）
* Differential Evolution（差分进化）
* Genetic Algorithm（遗传算法）

优先考虑适合连续、有边界、可能非光滑的黑箱目标函数的方法。

不要假设算法一定能够找到全局最优，应进行多次独立运行。

---

### Step 4：比较算法结果

对于不同算法记录：

* 最优遮蔽时间；
* 多次运行的平均遮蔽时间；
* 标准差；
* 运行时间；
* 目标函数调用次数；
* 收敛曲线。

使用相同或尽可能接近的计算预算进行比较。

---

### Step 5：对最终结果进行精细验证

得到最优策略后，不要直接认为结果正确。

使用更小的时间步长重新计算，例如：

dt = 0.01 s

并检查：

1. 三枚烟幕弹投放时间是否满足至少间隔 1 s；
2. 三枚烟幕弹的起爆时间和位置是否合理；
3. 三个烟幕是否按照规定的 3 m/s 下沉；
4. 烟幕是否只在起爆后的 20 s 内有效；
5. 三个烟幕的联合空间遮蔽判定是否正确；
6. 改变时间步长后最终遮蔽时间是否基本稳定。

---

# 九、程序输出

最终至少输出：

1. FY1 飞行方向 θ；
2. FY1 飞行速度 v；
3. 三枚烟幕弹的投放时间；
4. 三枚烟幕弹的投放位置；
5. 三枚烟幕弹的起爆时间；
6. 三枚烟幕弹的起爆位置；
7. 三枚烟幕各自的有效时间范围；
8. 三枚烟幕联合产生的总有效遮蔽时间；
9. 遮蔽时间段；
10. 优化算法的收敛过程；
11. 最终结果的验证结果。

---

![alt text](image.png)